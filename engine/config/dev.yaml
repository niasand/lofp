server:
  port: 4993
  telnet_port: 4000
  ssh_port: 4022
  frontend_url: "http://localhost:4992"

mongodb:
  uri: "${MONGODB_URI:mongodb://localhost:27017}"
  database: "lofp"

game:
  scripts_dir: "${LOFP_SCRIPTS_DIR:../original/scripts}"
  config_file: "${LOFP_CONFIG_FILE:../original/scripts/LEGENDS.CFG}"
  start_room: 3950
  bump_room: 201

auth:
  google_client_id: "${GOOGLE_CLIENT_ID:}"
  jwt_secret: "${JWT_SECRET:lofp-dev-secret-change-in-production}"

email:
  resend_api_key: "${RESEND_API_KEY:}"
  from_address: "${EMAIL_FROM:Legends of Future Past <noreply@lofp.metavert.io>}"

feedback:
  vibectl_url: "${VIBECTL_URL:}"
  vibectl_api_key: "${VIBECTL_API_KEY:}"

locale:
  default: "${LOCALE:en}"
